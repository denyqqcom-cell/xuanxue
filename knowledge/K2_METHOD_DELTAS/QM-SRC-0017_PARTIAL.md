# QM-SRC-0017 Partial Method Delta — 费秉勋《奇门遁甲新述》

Status: `PROVISIONAL / MID-READ / NO EMPIRICAL CREDIT / NOT FINAL DISTILLATE`

Date: 2026-08-21

Basis:

- sequential visual review currently through `PDF p164`;
- targeted later-page review at `p209, p264, p294, p354, p414-p419`;
- final book closure not yet complete.

This file is intentionally named `PARTIAL`. It must be revised or replaced after 419-page closure.

---

## Delta 1 — Source Position Separation

New correction:

`SOURCE_CONTAINS(X) != AUTHOR_ENDORSES(X)`

The source preserves large bodies of traditional克应/定局 material while its final critical volume rejects or questions several inherited components.

Therefore extraction must distinguish at least conceptually:

- `TRADITION_RECORDED`
- `AUTHOR_EXPLANATION`
- `AUTHOR_CRITIQUE`
- `AUTHOR_OPERATIONAL_COMMITMENT`

This is currently a reading discipline, **not** a new mandatory schema field.

Removal condition:

If existing Evidence/Distillate semantics already preserve this distinction reliably across sources, do not add new protocol complexity.

---

## Delta 2 — A simpler rival to Context Compression is now explicit

The source contains a large source-fixed lookup style:

`question/method family -> fixed table/role/pattern combination -> qualitative judgment`

This creates a serious rival model to the project’s current relational contextual method:

`SOURCE_FIXED_LOOKUP`

vs

`CONTEXT_FROZEN_RELATIONAL`

The project must not assume the second is superior because it sounds more flexible or intelligent.

Prospective comparison target:

- same unknown-outcome cases;
- same baseline exposure;
- same scoring;
- frozen before feedback;
- compare reproducibility, discrimination, calibration, abstention and complexity cost.

If the simpler lookup/role model performs equally or better, Context Compression must narrow.

---

## Delta 3 — Time-family similarity does not imply one algorithm

The source explicitly separates:

- 时家奇门;
- 年家奇门;
- 日家奇门;

while reusing common symbolic vocabulary.

Therefore:

`shared symbols != shared movement algorithm`

and:

`shared plate vocabulary != interchangeable time-family implementation`.

This supports keeping `time_family` as an explicit model object, but does not establish which family is empirically useful.

---

## Delta 4 — Traditional deterministic corpus can be internally disavowed

卷三 and the 540 fixed-bureau material contain many direct event claims. 卷十 later critiques omen-like “动应” and superstition-like accretions.

Therefore a deterministic statement should not receive extra weight merely because:

- it is specific;
- it is printed in a systematic table;
- it appears inside a book that elsewhere argues for rational analysis.

Specificity is not evidence strength.

This reinforces:

`DETERMINISTIC_CLAIM -> SOURCE ONLY until independently testable`.

---

## Delta 5 — Do not confuse critical rhetoric with empirical validation

The final critique calls for distinguishing truth from falsehood through deeper study and practice.

That stance is compatible with the project’s falsification discipline, but compatibility is not validation.

Do not write:

`author advocates verification -> our framework is validated`.

Correct statement:

`independent source meta-method shows skepticism toward blind inheritance`.

This may strengthen the historical/source rationale for testing, but gives `Empirical Support = 0`.

---

## Current theory action

`KEEP`:

- six-step `FRAME -> FREEZE -> RELATE -> DISCRIMINATE -> FALSIFY -> COMPRESS` skeleton;
- Source / Inference / Empirical separation;
- Context Compression as a falsifiable hypothesis.

`REVISE`:

- source reading now explicitly checks statement role and authorial position.

`TEST`:

- `SOURCE_FIXED_LOOKUP` vs `CONTEXT_FROZEN_RELATIONAL`.

`NO-OP`:

- no v0.4;
- no new mandatory schema field;
- no predictive validity claim;
- no universal adoption of this book’s tables.

---

## What could still reverse this delta

The remaining sequential pages may show that:

- the final critique applies only to narrow omen material, not the broader lookup corpus;
- earlier sections define conditional relations more richly than currently recognized;
- a different setup/time object changes the apparent rival-model comparison;
- the author’s preserved tables are editorial/reference material with a different intended status.

Therefore this delta remains provisional until full closure.