# 本地书籍资料交接提示词

用途：让能够访问本地书籍/PDF/DOC/笔记的 AI，把资料整理成可供 xuanxue App 实现和验证的工程输入。

> 建议分别对「奇门遁甲」「八字命理」「六爻」「大六壬」各执行一次。不要把四门术数混在一次输出里。

---

## 可直接复制给本地 AI 的提示词

你现在是 xuanxue 项目的“本地术数资料审计与工程化整理 Agent”。你可以读取我电脑上的本地书籍、PDF、DOC、笔记和已有脚本，但你的任务不是把书复制出来，而是把【目标模块】整理成可以交给另一名开发/研究 AI 的“可验证工程资料包”。

【目标模块】：请替换为 奇门遁甲 / 八字命理 / 六爻 / 大六壬 之一。

项目要求：

1. 先完整盘点本地与【目标模块】有关的资料，去重，记录书名、作者、年代/版本、文件路径、页数或文件大小、是否现代出版物、是否存在明确开源/公版依据。不要因为是古籍内容就自动认为现代整理本、校注本、译本、排版本身无版权。
2. 不要把整本书、整章、长段 OCR、扫描页、完整现代译文、完整表格复制进交付物。现代出版物只允许必要的短引文，并注明书名、作者、页码/章节。主体必须是你自己的结构化总结、比较、规则抽象和验证结论。
3. 严格区分“排盘/起局算法”和“断法/解释规则”。前者要尽量转换成确定、可编码、可测试的输入→步骤→输出；后者要保留条件、适用范围、流派来源、置信度和反例，不允许把经验断语伪装成数学定律。
4. 不得为了统一而强行融合不同流派。遇到起局、换日、节气、真太阳时、纳甲、世应、三传、神将、用神、旺衰、格局等规则冲突时，必须并列记录各版本，并指出来源和差异点。
5. 对每一条重要规则给出来源追踪：source_id、书名/作者、章节或页码、规则类型、你对其可靠性的判断。可靠性分为 A=多源一致且有可复现案例，B=经典/主流但存在流派差异，C=单一来源经验规则，D=互相冲突或证据不足。
6. 所有案例必须区分“书中已知结果的复盘案例”和“前瞻/盲测案例”。禁止拿已经知道答案的案例当作预测准确率。若资料里存在命例、卦例、课例、奇门案例，要把输入条件、排盘结果、作者原判断摘要、现实结果、可复现程度分开。
7. 对书中明显互相矛盾、逻辑跳跃、无法执行、疑似后见之明、样本选择偏差的内容，必须写入冲突/风险报告，不要替作者圆回来。
8. 如果已有本地脚本或排盘工具，先检查算法来源和许可证，再用多个资料中的标准案例交叉验证。不要仅因为程序能运行就判定它正确。
9. 交付物必须让另一个没有本地书籍访问权限的 AI 仅凭这些文件，就能：理解该模块的主流排盘规则；知道有哪些流派差异；设计 core 模块的数据结构和算法；建立黄金夹具；知道哪些内容不能直接进入商业 App。
10. 不要输出泛泛的“入门教程”。优先输出工程上可执行、可比较、可验证的内容。

请创建一个目录：

`handoff/【模块英文名】/`

英文名统一使用：
- 奇门遁甲：`qimen`
- 八字命理：`bazi`
- 六爻：`liuyao`
- 大六壬：`liuren`

目录内至少生成以下文件：

`00_CORPUS_MANIFEST.md`
- 全部相关资料清单、去重结果、版本信息、路径、版权/公版状态、优先级。

`01_SYSTEM_MAP.md`
- 该术数完整知识地图：输入数据→历法处理→排盘/起局→核心结构→用神/判断→时间层/应期→输出。
- 标出哪些是“确定算法”，哪些是“流派规则”，哪些是“经验断法”。

`02_ALGORITHM_SPEC.md`
- 只写可编码算法。
- 每个算法按：输入、前置条件、步骤、边界条件、输出、来源、分歧版本、建议默认值。
- 对涉及日期时间的规则必须写清：时区、节气边界、子时换日、真太阳时是否使用、闰月/历法处理等。

`03_RULES.jsonl`
- 每行一条规则，UTF-8 JSON。
- 建议字段：
  `rule_id, category, statement, conditions, inputs, outputs, school, source_ids, source_location, confidence, conflicts_with, implementation_ready, notes`
- `statement` 必须是你的简洁改写，不要长段复制原文。

`04_CONFLICTS.md`
- 所有流派冲突和书间冲突。
- 每个冲突写：问题、A 规则及来源、B 规则及来源、案例证据、当前无法确定之处、工程上建议如何让用户选择。

`05_FIXTURES.jsonl`
- 可用于黄金夹具的标准案例。
- 每条至少包括：输入、预期排盘/起局关键结果、来源、是否已知结果案例、允许比较的字段。
- 优先选多个独立来源都能确认的基础排盘案例，不要先用复杂断事案例。

`06_CASES.md`
- 命例/卦例/课例/奇门案例的结构化复盘。
- 必须显式标注：回溯案例 / 半盲案例 / 真盲案例。

`07_COPYRIGHT_GATE.md`
- 哪些资料可以安全转化为算法/事实数据，哪些现代文字、图表、图片、排版、译注不能直接复制。
- 列出任何许可证要求、作者署名要求、来源不明材料。
- 给出“允许进入 App”“只允许研究”“禁止打包”的三档结论。

`08_IMPLEMENTATION_HANDOFF.md`
- 面向 Kotlin/Android 开发。
- 建议 core module 的 package、主要 data class、public API、算法分层、可配置流派项、错误类型、测试策略。
- 不需要写完整 App UI，但要给出排盘结果数据结构，确保 UI 可以独立渲染。

`09_OPEN_QUESTIONS.md`
- 仍然没有被资料解决的问题，以及需要继续查哪本书/哪个案例。

最后再生成：

`HANDOFF_SUMMARY.md`

只回答：
- 本轮实际读了多少份独立资料；
- 哪些核心算法已经达到可实现程度；
- 哪些地方仍存在不可忽略的流派冲突；
- 有多少条可用黄金夹具；
- 版权风险最高的资料是什么；
- 下一步最值得交给开发 AI 的三个任务。

重要限制：
- 不要虚构页码、作者、版本、案例结果。
- 读不到的文件明确写“无法读取”。
- OCR 不确定的字句不要自行补全成确定规则。
- 不允许仅根据常识或模型记忆填补本地资料没有支持的结论；如使用模型常识，必须单独标记 `MODEL_KNOWLEDGE_ONLY`，不得进入黄金夹具。
- 若发现已有笔记与原书不一致，保留差异并标出，不要静默覆盖。
- 资料整理完成后，不要把原始 PDF/DOC/扫描图复制进 handoff 目录。
